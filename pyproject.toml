[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gway-wire"
version = "0.3.1"
description = "Endpoint connectivity for Gway devices, with WireGuard as the first backend"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
    "gway-web @ git+https://github.com/arthexis/gway-web.git@5cc18f0756e289a65f3dc2385b8af7f8fa3459a4",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501", "I001"]
