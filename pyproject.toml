[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gway-wire"
version = "0.3.1"
description = "Endpoint connectivity for Gway devices, with WireGuard as the first backend"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
    "gway-web @ git+https://github.com/arthexis/gway-web.git@32ebf64e7c050c26fa48c9baf73fa5eda1063d44",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501", "I001"]
